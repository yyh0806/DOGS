__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
install/
log/
models/
deps/
*.jpg
*.png
*.bin
*.pt
*.engine
*.onnx
.env

# HIGH-3 例外: mock_person.png 是阶段B mock 视频必需资源 (COCO 人物裁图, 让 YOLO 在
# mock 模式真检出 person, spec-stage-b §8.1/C4.4)。10KB 小图, 必须入库,
# 否则 git clone/pull 的部署路径 (非 deploy_nx_ai.sh) 会缺该文件。
!go2w_search_ws/web/static/mock_person.png
.omc/

# 工具缓存 (不在仓库内但常出现在仓库根: locate-anything.cpp 上游克隆 / playwright MCP)
.cache/
.playwright-mcp/
.playwright-cli/

# 运行时产物: mission report 存储 / costmap 快照 / pytest 缓存 / 调试输出
costmap.pgm
.pytest_fastlio_tmp/
go2w_search_ws/web/missions/
tmp/
go2w_search_ws/tmp/

# 根目录调试产物: TF 帧图 / API 状态快照 / 界面截图 (均为人工排查时导出, 不入库)
frames_*.gv
frames_*.pdf
status.json
plan-check.png
search-panel.png
web-check-8000.png
*.log

# 仿真 SIGFPE workaround 编译产物 (WSL2 glibc ifunc): 源码 mycos.c + mycos.map 入库,
# .so 由目标机现场 gcc 编译 (见 QUICKSTART.md "仿真模式" Step 4)
go2w_search_ws/tools/mycos.so

# lake_loop 原型 (2026-08 入库): Python 环境 / 瓦片与渲染缓存 / 任务产物不入库
lake_loop/.venv/
lake_loop/cache/
lake_loop/runs/

# 部署依赖 tar 包 (347MB, 可由 vendor 源重打包) 与根目录临时文件
go2w_search_ws/fast_lio_deps.tar.gz
.reasonix/
tmp_pdf.py

# vendor 排除 (来源与获取方式见 src/VENDORED.md):
# FAST_LIO 上游文档媒体 (127MB 论文/实验gif, 非构建必需)
go2w_search_ws/src/FAST_LIO_ROS2/doc/
# livox 仿真扫描模式表 (164MB 上游数据, 首次仿真前 tools/fetch_livox_scan_modes.sh 获取)
go2w_search_ws/src/ros2_livox_simulation/scan_mode/
